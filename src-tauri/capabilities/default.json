{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permission set for JARVIS's main window. Scoped by actually grepping every @tauri-apps/* import in src/ (see NOTES below) rather than copied from a template or granted broadly to 'make it work'.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
